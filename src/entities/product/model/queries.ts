// // entities/product/model/queries.ts
// import { useQuery } from '@tanstack/react-query';
// import { api } from '@/shared/api/http';

// export const useProductsQuery = () =>
//   useQuery(['products'], async () => (await api.get('/products')).data);
